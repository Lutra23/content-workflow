# memory-recall 🦦

Quick CLI to search and recall saved memories from git-notes-based memory system.

## Quick Start

```bash
cd memory-recall
python main.py --help     # Show usage
python main.py            # List all memories overview
```

## Usage

```bash
# Overview of all memories
python main.py

# Search by keyword
python main.py --search 核心目标

# Show recent N memories
python main.py --recent 5

# Filter by type (decision/preference/learning/task/info)
python main.py --type decision

# Show critical memories only
python main.py --critical

# List all topics with counts
python main.py --topics

# Install globally
python main.py --install
```

## Features

- 🔍 Keyword search across all memories
- 📅 Recent memories with timestamps
- 🏷️ Topic/entity browsing
- 🔴 Critical memory highlighting
- 📊 Memory type filtering
- 📈 Memory overview stats

## Install

```bash
# Install to ~/.local/bin
python main.py --install

# Add to PATH if needed
export PATH="$HOME/.local/bin:$PATH"
```

---

*Generated by Nightly Project Builder*
*Date: 2026-01-31*
